apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-linkding
  namespace: linkding
spec:
  rules:
    - http:
        paths:
          - path: /linkding
            pathType: Prefix
            backend:
              service:
                name: linkding
                port:
                  number: 9090
